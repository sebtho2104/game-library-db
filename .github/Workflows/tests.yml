name: Run tests

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres
        env:
          POSTGRES_PASSWORD: passord123
          POSTGRES_DB: game_library
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
      - run: mvn compile exec:java -Dexec.mainClass="com.sebastian.gamelibrary.SchemaSetup"
      - run: mvn compile exec:java -Dexec.mainClass="com.sebastian.gamelibrary.SeedData"
      - run: mvn test